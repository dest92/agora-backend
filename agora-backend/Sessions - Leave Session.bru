meta {
  name: Sessions - Leave Session
  type: http
  seq: 23
}

post {
  url: {{BASE_URL}}/sessions/{{SESSION_ID}}/leave
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should confirm leave", function() {
    const body = res.getBody();
    expect(body.left).to.equal(true);
  });
}
