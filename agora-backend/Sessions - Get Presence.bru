meta {
  name: Sessions - Get Presence
  type: http
  seq: 24
}

get {
  url: {{BASE_URL}}/sessions/{{SESSION_ID}}/presence
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should contain users array", function() {
    const body = res.getBody();
    expect(body.users).to.be.an("array");
  });
}
