meta {
  name: Assignees - Remove Assignee
  type: http
  seq: 18
}

delete {
  url: {{BASE_URL}}/boards/{{BOARD_ID}}/cards/{{CARD_ID}}/assignees/{{USER_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should confirm removal", function() {
    const body = res.getBody();
    expect(body.removed).to.equal(true);
  });
}
