meta {
  name: Tags - Assign Tag
  type: http
  seq: 15
}

post {
  url: {{BASE_URL}}/boards/{{BOARD_ID}}/cards/{{CARD_ID}}/tags/{{TAG_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{JWT_TOKEN}}
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should confirm assignment", function() {
    const body = res.getBody();
    expect(body.assigned).to.equal(true);
  });
}
