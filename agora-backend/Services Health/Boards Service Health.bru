meta {
  name: Boards Service Health
  type: http
  seq: 2
}

get {
  url: {{BOARDS_URL}}/health
  body: none
  auth: none
}

tests {
  test("Boards Service should be running on port 3011", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should respond within 5 seconds", function() {
    expect(res.getResponseTime()).to.be.below(5000);
  });
}
