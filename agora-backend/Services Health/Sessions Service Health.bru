meta {
  name: Sessions Service Health
  type: http
  seq: 4
}

get {
  url: {{SESSIONS_URL}}/health
  body: none
  auth: none
}

tests {
  test("Sessions Service should be running on port 3013", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should respond within 5 seconds", function() {
    expect(res.getResponseTime()).to.be.below(5000);
  });
}
